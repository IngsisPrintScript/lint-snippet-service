spring:
  application:
    name: ingsis
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5433/linter}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${AUTH0_ISSUER_URI}
  data:
    redis:
      host: ${REDIS_HOST:snippet_redis}
      port: ${REDIS_PORT:6379}

okta:
  oauth2:
    # Replace with the domain of your Auth0 tenant.
    issuer: ${AUTH0_ISSUER_URI}
    # Replace with the API Identifier for your Auth0 API.
    audience: ${AUTH0_AUDIENCE}

server:
  port: ${API_PORT:8085}
  address: 0.0.0.0

redis:
  streams:
    lintRequest: ${REDIS_STREAM_REQUEST_LINT_KEY}
    lintResult: ${REDIS_STREAM_RESULT_LINT_KEY}
  groups:
    lint: ${REDIS_GROUP_LINT_KEY}

bucket:
  url: ${BUCKET_URL}